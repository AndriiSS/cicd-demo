name: Sequential Jobs

on: push

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
    - run: echo "Building..."
  
  test:
    runs-on: ubuntu-24.04
    needs: build
    steps:
    - run: echo "Testing..."
  
  deploy:
    runs-on: ubuntu-24.04
    needs: [build, test]
    steps:
    - run: echo "Deploying..."

